<div class="row">
   <% @items.each do |item| %>
     <div class="col-md-4 col-sm-6">
       <%= image_tag item.image.url(:medium) %>
       <div>
         <%= item.name %>
       </div>
     </div>
   <% end %>
</div>
